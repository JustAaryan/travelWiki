<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TravelWiki</title>
    <link rel="stylesheet" href="/head.css">
    <link rel="stylesheet" href="/body.css">
    <link rel="stylesheet" href="/signin.css">
    <link rel="stylesheet" href="/animation.css">
    <link rel="stylesheet" href="/textanimation.css">
    <link rel="stylesheet" href="/footer.css">

</head>

<body>
    <div class="slideshow-container">
        <img class="mySlides active" src="ktm.jpeg" alt="Error Kathmandu">
        <img class="mySlides" src="pok.jpeg" alt="Error Pokhara">
        <img class="mySlides" src="must.jpeg" alt="Error Mustang">
        <img class="mySlides" src="bhak.jpeg" alt="Error Bhaktapur">

        <header class="navbar">
            <h1>TravelWiki</h1>
            <!-- <a href="./login.ejs">
            <button class="signin">Sign in</button>
            </a> -->
            <nav>
                <a href="#home">Home</a>
                <a href="blog.ejs">Places</a>
                <a href="fesabout.ejs">Festivals</a>
                <a href="socials.ejs">About Us</a>
            </nav>
        </header>
    
       

        <div class="button-container">
            <button onclick="button()">Search</button>

            <div class="dropdowns">
                <select id="dropdown1">
                    <option value="">Select the Occasion</option>
                    <option value="Family Trip">Family Trip</option>
                    <option value="Children Trip">Children Trip</option>
                    <option value="Couple Date">Couple Date</option>
                    <option value="Relaxing Trip">Relaxing Trip</option>
                </select>

                <select id="dropdown2">
                    <option value="">Select No of People</option>
                    <option value="1">1</option>
                    <option value="2">2</option>
                    <option value="3">3</option>
                    <option value="4">4</option>
                </select>

                <div id="results"></div>
            </div>
        </div>

        <div id="text-container"></div>

    </div>

   
  
       
       
        
   <!--header>
        <h1>TravelWiki</h1>
         <a href="./login.ejs">
        <button class="signin">Sign in</button>
        </a>
    </header>

    <nav class="navbar">
        <a href="#home">Home</a>
        <a href="blog.ejs">Places</a>
        <a href="fesabout.ejs">Festivals</a>
        <a href="socials.ejs">About Us</a>
    </nav-->

    <!-- <footer> /* font-size: clamp(36px,8vw,65px);
        <p>&copy; Aryan Travelwiki Design. All rights reserved.</p>
    </footer>-->
    <div id="text-container"></div>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script>
       // JavaScript for slideshow functionality
       var texts = [
             "Welcome to our website.",
            "Popular Destinations",
            "Your own Preferences.",
            "Different Festivals",
            "Different Occasions",
             "All in One"
            // Add more texts as needed
        ];

        var currentTextIndex = 0;
        var currentText = '';
        var letterIndex = 0;

        function typeText() {
            if (letterIndex < texts[currentTextIndex].length) {
                currentText += texts[currentTextIndex][letterIndex];
                document.getElementById('text-container').innerHTML = currentText + '|';
                letterIndex++;
                setTimeout(typeText, 50); // Adjust the typing speed here
            } else {
                setTimeout(eraseText, 1000); // Wait before erasing
            }
        }

        function eraseText() {
            if (letterIndex > 0) {
                currentText = currentText.slice(0, -1);
                document.getElementById('text-container').innerHTML = currentText + '|';
                letterIndex--;
                setTimeout(eraseText, 30); // Adjust the erasing speed here
            } else {
                currentTextIndex = (currentTextIndex + 1) % texts.length;
                setTimeout(typeText, 500); // Wait before typing the next text
            }
        }

        function showSlides() {
            var slides = document.getElementsByClassName("mySlides");

            for (var i = 0; i < slides.length; i++) {
                slides[i].style.opacity = 0;
            }

            slideIndex++;
            if (slideIndex > slides.length) {
                slideIndex = 1;
            }

            slides[slideIndex - 1].style.opacity = 1;

            setTimeout(showSlides, 3000); // Change 3000 to your desired interval
        }
        // function button()
        // {
        //     window.location.href ='login.ejs';
        //     alert('Sign in');
        // }

        var slideIndex = 0;
        showSlides();
        typeText(); // Start the text typing animation

      
    //dropdown closes when clicked elsewhere.
       window.addEventListener('click', function(event) {
            const dropdowns = document.querySelectorAll('.dropdown');
            dropdowns.forEach(dropdown => {
                if (!dropdown.contains(event.target)) {
                    const menus = dropdown.querySelectorAll('.menu');
                    menus.forEach(menu => {
                        menu.style.display = 'none';
                    });
                }
            });
        });
        function button() {

         // Get the selected values from the dropdowns
    var dp1 = document.getElementById("dropdown1").value;
    var dp2 = document.getElementById("dropdown2").value;

    // Perform actions based on the selected values
    if (dp1 && dp2) {
        // Redirect the user to a specific page or perform other actions based on dp1 and dp2
        window.location.href = '/blog.ejs?dp1=' + dp1 + '&dp2=' + dp2;
    } else {
        // Handle the case when dropdowns are not selected
        alert("Please select values from both dropdowns.");
    }
        }

       

    </script>
    

</body>

</html>

          
        
